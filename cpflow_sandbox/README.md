# CPFlow Sandbox

This folder isolates the small supplementary compile-target experiment.

- Scripts in this folder may import repository modules such as `shiftflow`.
- Outputs generated from this sandbox should stay inside this folder.
- The goal is a small implementation-side experiment for ShadowFluid reduced
  compilation targets, not a new system or a large benchmark.
